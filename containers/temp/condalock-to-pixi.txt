pixi global install vim
cd /work/
mkdir temp
cd temp
cp ../conda/seurat-bioc-environment-2025.yaml .
pixi init -i seurat-bioc-environment-2025.yaml 
# check conda channels and remove the ones not needed
pixi upgrade
