// Get the element by its class names
var element = document.querySelector('.callout-tip .callout-header .callout-title-container');

// Check if the element exists
if (element) {
  // Apply the CSS properties
  element.style.letterSpacing = '0.1em';
  element.style.fontSize = '0.85em';
  element.style.fontWeight = '600';
}